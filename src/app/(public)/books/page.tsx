import BookList from '@/components/BookList';

const BooksPage = () => {
    return (
        <div className="container mx-auto p-4">
            <BookList />
        </div>
    );
};

export default BooksPage;
